typedef struct car_t{
    char make[20];
    char model[20];
    char color[20];
    int year;
} car;

// typedef <what to typedef> <what to call it>;
// struct <struct name> {<attributes>} <optional struct "nickname">;

int main() {
    struct car;
}