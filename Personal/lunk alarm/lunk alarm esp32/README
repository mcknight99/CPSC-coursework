cmd.exe in the platformio project directory (has /data) with upload COM open
    pio run -t uploadfs

be wary of not exceeding available flash memory size - do in the custom partition table


TODO:
- password override lock/unlock
- "print" function that serial's AND sends to the server and displays on the html (allows for)
